<view class="i-index-item i-class" >
    <view class="i-index-item-header" wx:if="{{name!='cur' && name!='loc' && name!='hot' && searchkey.length==0}}">{{name}}</view>
    <view class="i-index-item-content">
        <slot></slot>
    </view>
</view>
<wxs module="parse">
    module.exports = {
       
    }
</wxs>
